<%- model_class = Article -%>
<% page_title t('app.actions.with_obj.new',
                obj: t('activerecord.models.article')) %>

<% if user_signed_in? %>
  <div class='actions-container'>
    <%= render partial: 'shared/new_actions',
          locals: {
            object: @article,
            index_path: articles_path
                  }
    %>
  </div>
<% end %>
<%= render :partial => 'form' %>
