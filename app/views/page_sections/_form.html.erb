<%= semantic_form_for @page_section do |f| %>
  <%= f.semantic_errors *f.object.errors.keys %>
  <%= f.inputs do %>
    <% if can? :create, PageSection %>
      <%= f.input :name %>
    <% end %>
    <%= f.input :label %>
    <%= f.input :content, input_html: { class: 'tinymce' } %>
  <% end %>

  <%= f.actions do %>
    <%= f.action :submit, button_html: { class: 'btn btn-success' } %>
  <% end %>
<% end %>
