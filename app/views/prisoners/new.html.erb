<%- model_class = Prisoner -%>
<% page_title t('app.actions.admin.with_obj.new', obj: t('activerecord.models.prisoner')) %>

<% if user_signed_in? %>
  <div class='actions-container'>
    <%= render partial: 'shared/new_actions',
          locals: {
            object: @prisoner,
            index_path: prisoners_path,
                  }
    %>
  </div>
<% end %>

<%= render :partial => 'form' %>
