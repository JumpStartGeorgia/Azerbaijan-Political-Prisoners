<%- model_class = User -%>
<% page_title "Edit #{model_class.model_name.human.titleize}" %>

<% if user_signed_in? %>
  <div class='actions-container'>
    <%= render partial: 'shared/edit_actions',
               locals: {
                 object: @user,
                 index_path: users_path,
                 show_path: user_path
               }
    %>
  </div>
<% end %>

<%= render partial:'form' %>
